// Creating an object based off of the users input
function addUser() {
	var User = {first_name:"", last_name:"",  age:""};
	
	// Display that data from the object
	document.getElementById("demo").innerHTML = "User Info: " + User.type;
